<div class="ui left aligned divided list">
{{#each this}} 
  <div class="item">
    <div class="left floated content">
    <div class="ui button meet" data-lat="{{lat}}" data-lng="{{lng}}"><i class="user icon"></i>{{first_name}}</div>
    </div>
    </div>
   {{/each}}
  </div>
 </div>

